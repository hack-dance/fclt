{"actor":"hack@mac.lan","eventId":"bf23c7b9-37ed-4557-9ce9-211e64d1fe38","payload":{"body":"## Summary\nEnhance the canonical MCP and skill schemas to support lossless round-trip translation between tool formats.\n\n## Context\nFrom Council Deliberation (2026-01-29): \"Canonical format should be a superset with explicit mapping to tool formats and field preservation for round-trip.\"\n\n## Requirements\n\n### vendorExtensions field\nAdd a `vendorExtensions` field to MCP server schema for unknown/tool-specific fields that don't map to canonical properties. This ensures we don't lose data when parsing tool configs.\n\n```typescript\nmcp: {\n  servers: {\n    [name: string]: {\n      // ... existing fields ...\n      vendorExtensions?: Record<string, unknown>;  // NEW\n    }\n  }\n}\n```\n\n### provenance field\nTrack where each item came from:\n\n```typescript\nprovenance: {\n  source: string;        // 'cursor', 'claude-desktop', etc.\n  sourcePath: string;    // Original file path\n  importedAt: string;    // ISO timestamp\n  sourceHash?: string;   // Hash of original content for diff detection\n}\n```\n\n### Implementation\n1. Update `src/scan.ts` to preserve unknown fields during MCP parsing\n2. Update type definitions in a new `src/types.ts` or existing types\n3. Update index builder to include new fields\n4. Add tests for round-trip preservation\n\n## Acceptance Criteria\n- [ ] Unknown MCP fields preserved in vendorExtensions\n- [ ] Provenance tracked for all consolidated items\n- [ ] Round-trip test: parse → canonical → generate → parse yields identical result\n\n## Effort\n1–2 days\n\n## Phase\nPhase 3: Normalize & Index","status":"open","title":"T-001: Canonical schema enhancements"},"projectId":"035d2b4054bf","projectName":"tackle-box","ticketId":"T-00001","ts":1769706780,"tsIso":"2026-01-29T17:13:00.000Z","type":"ticket.created"}
{"actor":"hack@mac.lan","eventId":"f85418d4-31c6-4dbe-abe6-4f2fedbb1c60","payload":{"body":"## Summary\nBuild the index builder that scans `~/agents/.tb/` and generates a queryable `index.json`.\n\n## Context\nThe index is the foundation for all query, enable/disable, and audit operations. It needs to be fast, accurate, and include rich metadata.\n\n## Requirements\n\n### Create src/index-builder.ts\nCore module that:\n1. Scans `~/agents/.tb/skills/` for skill directories (each containing SKILL.md)\n2. Scans `~/agents/.tb/mcp/servers.json` for MCP entries\n3. Scans `~/agents/.tb/agents/` for agent profile files\n4. Scans `~/agents/.tb/snippets/` for snippet files\n\n### SKILL.md parsing\nExtract metadata from SKILL.md:\n- Description (from frontmatter `description:` or first paragraph)\n- Tags (from frontmatter `tags:` if present)\n- Name (from directory name)\n\n### Index schema\nWrite to `~/agents/.tb/index.json`:\n\n```typescript\ninterface TackleboxIndex {\n  version: number;\n  updatedAt: string;\n  skills: { [name: string]: SkillEntry };\n  mcp: { servers: { [name: string]: McpEntry } };\n  agents: { [name: string]: AgentEntry };\n  snippets: { [name: string]: SnippetEntry };\n}\n```\n\n### Commands\n- `tacklebox index` — rebuild index from current state\n- `tacklebox index --force` — full reindex, clear any cached state\n\n### CLI integration\nUpdate `src/index.ts` to add the `index` command.\n\n## Acceptance Criteria\n- [ ] Index correctly lists all skills with descriptions\n- [ ] Index correctly lists all MCP servers\n- [ ] Index includes lastModified timestamps\n- [ ] `tacklebox index` completes in < 2s for typical installs\n- [ ] Index file is human-readable JSON\n\n## Effort\n2 days\n\n## Phase\nPhase 3: Normalize & Index\n\n## Dependencies\n- T-001 (schema enhancements) should be complete for full provenance","status":"open","title":"T-002: Index builder implementation"},"projectId":"035d2b4054bf","projectName":"tackle-box","ticketId":"T-00002","ts":1769706804,"tsIso":"2026-01-29T17:13:24.000Z","type":"ticket.created"}
{"actor":"hack@mac.lan","eventId":"666b9350-69fe-4250-b0a7-448a8bcf5ea1","payload":{"body":"## Summary\nImplement query engine for filtering/searching the index, plus `list` and `show` CLI commands.\n\n## Context\nUsers need to quickly find and inspect skills, MCP servers, and agents. The query engine powers all filtering operations.\n\n## Requirements\n\n### Create src/query.ts\nQuery engine with:\n- Filter by type: `skills`, `mcp`, `agents`, `snippets`\n- Filter by enabled-for: `--enabled-for cursor`\n- Filter by audit status: `--untrusted`, `--flagged`\n- Filter by tags: `--tag automation`\n- Full-text search in names and descriptions\n\n### list command\n```bash\ntacklebox list                    # List all (summary)\ntacklebox list skills             # List skills only\ntacklebox list mcp                # List MCP servers\ntacklebox list agents             # List agent profiles\ntacklebox list snippets           # List snippets\n\n# Filters\ntacklebox list --enabled-for cursor\ntacklebox list --untrusted\ntacklebox list --flagged\ntacklebox list --json             # JSON output\n```\n\nOutput format (human):\n```\nSkills (12)\n  github          GitHub CLI integration          cursor, claude\n  weather         Weather forecasts               cursor\n  ...\n\nMCP Servers (5)\n  filesystem      File system access              cursor, claude\n  ...\n```\n\n### show command\n```bash\ntacklebox show github             # Show skill details\ntacklebox show mcp:filesystem     # Show MCP server (mcp: prefix)\ntacklebox show --json github      # JSON output\n```\n\nOutput includes:\n- Full description\n- Source/provenance\n- Enabled-for tools\n- Trust/audit status\n- File contents (SKILL.md or server config)\n\n## Acceptance Criteria\n- [ ] `list` shows all items with key metadata\n- [ ] `list skills` filters correctly\n- [ ] `--enabled-for` filter works\n- [ ] `show` displays full details including content\n- [ ] `mcp:` prefix disambiguates MCP servers\n- [ ] JSON output available for scripting\n\n## Effort\n1–2 days\n\n## Phase\nPhase 3: Normalize & Index\n\n## Dependencies\n- T-002 (index builder)","status":"open","title":"T-003: Query engine and list/show commands"},"projectId":"035d2b4054bf","projectName":"tackle-box","ticketId":"T-00003","ts":1769706827,"tsIso":"2026-01-29T17:13:47.000Z","type":"ticket.created"}
{"actor":"hack@mac.lan","eventId":"819ab1bd-c1d7-4d08-991b-392f5df215cb","payload":{"body":"## Summary\nImplement conservative conflict resolution during consolidation with diff preview and user choice.\n\n## Context\nFrom Council Deliberation: \"Auto-merge only when normalized content hashes match. Otherwise, interactive prompt with diff and options (keep-newest, keep-local, keep-remote, keep-both).\"\n\nThis is critical for safety — we must not silently lose user customizations or merge malicious content.\n\n## Requirements\n\n### Content normalization\nBefore comparing, normalize content:\n- Trim leading/trailing whitespace\n- Normalize line endings to LF\n- Optionally: normalize JSON formatting for config files\n\n### Hash comparison\n```typescript\nfunction contentHash(content: string): string {\n  const normalized = normalize(content);\n  return Bun.hash(normalized).toString(16);\n}\n```\n\n### Conflict detection\nWhen consolidating an item that already exists:\n1. Compare normalized hashes\n2. If identical → auto-merge silently\n3. If different → show conflict UI\n\n### Conflict UI (clacks)\n```\n⚠️  Conflict: skill \"github\" exists with different content\n\n  Current (consolidated):\n    Path: ~/agents/.tb/skills/github/SKILL.md\n    Modified: 2026-01-15 10:30\n    Size: 2.4 KB\n\n  Incoming (from cursor):\n    Path: ~/.cursor/skills/github/SKILL.md\n    Modified: 2026-01-28 14:20\n    Size: 2.6 KB\n\n  [v] View diff\n  [1] Keep current (skip incoming)\n  [2] Keep incoming (overwrite)\n  [3] Keep both (rename incoming to github-cursor)\n  [c] Cancel\n```\n\n### Diff preview\nWhen user selects \"View diff\":\n- Show unified diff\n- Highlight additions/deletions\n- Return to choice prompt after viewing\n\n### --auto modes\nFor scripting/batch operations:\n```bash\ntacklebox consolidate --auto keep-newest   # Keep most recently modified\ntacklebox consolidate --auto keep-current  # Skip all conflicts\ntacklebox consolidate --auto keep-incoming # Overwrite all\n```\n\n### Archive originals\nWhen overwriting, archive the replaced version:\n- `~/agents/.tb/skills/github/SKILL.md.bak.2026-01-29`\n- Or in a separate archive directory\n\n## Acceptance Criteria\n- [ ] Identical normalized content auto-merges silently\n- [ ] Different content triggers interactive prompt\n- [ ] Diff preview shows clear comparison\n- [ ] All choice options work correctly\n- [ ] `--auto` modes work for batch operations\n- [ ] Replaced files are archived, not deleted\n\n## Effort\n2–3 days\n\n## Phase\nPhase 4: Managed Mode & Sync\n\n## Security Note\nFrom Council: \"Avoid auto-merging semantic changes. Merging could hide malicious inserts; require explicit review for differences.\"","status":"open","title":"T-004: Consolidation conflict workflow"},"projectId":"035d2b4054bf","projectName":"tackle-box","ticketId":"T-00004","ts":1769706854,"tsIso":"2026-01-29T17:14:14.000Z","type":"ticket.created"}
{"actor":"hack@mac.lan","eventId":"3abbddd5-0b23-4765-a66d-08c7e74ee95f","payload":{"body":"## Summary\nImplement \"managed mode\" where tacklebox takes control of a tool's skill and MCP configuration via backup + symlink pattern.\n\n## Context\nFrom spec: \"Per-tool opt-in to let tacklebox manage. Backup existing, symlink to central, tacklebox writes tool-specific configs from central state.\"\n\nThis is the core value prop — one source of truth, many tools consume it.\n\n## Requirements\n\n### manage command\n```bash\ntacklebox manage cursor\n```\n\nFlow:\n1. Check if tool is already managed → error if so\n2. Detect tool paths (use adapter, see T-007/T-008)\n3. Backup existing:\n   - `~/.cursor/skills/` → `~/.cursor/skills.bak/`\n   - `~/.cursor/mcp.json` → `~/.cursor/mcp.json.bak`\n4. Create skills directory if needed\n5. Create per-skill symlinks for enabled skills:\n   - `~/.cursor/skills/github` → `~/agents/.tb/skills/github`\n6. Generate tool-specific `mcp.json` from canonical `servers.json`\n7. Record in `~/.tacklebox/managed.json`\n\n### unmanage command\n```bash\ntacklebox unmanage cursor\n```\n\nFlow:\n1. Check if tool is managed → error if not\n2. Remove symlinks\n3. Restore from backup (if exists)\n4. Remove from `managed.json`\n\n### managed command\n```bash\ntacklebox managed\n```\n\nOutput:\n```\nManaged tools:\n  cursor      Skills: 8 enabled, MCP: 5 servers\n  claude      Skills: 12 enabled, MCP: 5 servers\n```\n\n### managed.json schema\n```json\n{\n  \"version\": 1,\n  \"tools\": {\n    \"cursor\": {\n      \"managedAt\": \"2026-01-29T...\",\n      \"skillsPath\": \"~/.cursor/skills\",\n      \"mcpPath\": \"~/.cursor/mcp.json\",\n      \"backups\": {\n        \"skills\": \"~/.cursor/skills.bak\",\n        \"mcp\": \"~/.cursor/mcp.json.bak\"\n      }\n    }\n  }\n}\n```\n\n### Per-skill symlinks\nFor granular enable/disable:\n- Each enabled skill gets its own symlink\n- Disabled skills have no symlink\n- This allows per-tool skill selection\n\n### Safety checks\n- Verify backup succeeded before modifying\n- Check for existing symlinks (don't double-manage)\n- Validate tool paths exist\n- Warn if tool is running (optional)\n\n## Acceptance Criteria\n- [ ] `manage cursor` backs up and symlinks correctly\n- [ ] `unmanage cursor` restores from backup\n- [ ] `managed` lists all managed tools\n- [ ] Per-skill symlinks work (not directory symlink)\n- [ ] Backup restoration is reliable\n- [ ] Error handling for permission issues\n\n## Effort\n2–3 days\n\n## Phase\nPhase 4: Managed Mode & Sync\n\n## Dependencies\n- T-007 (adapter architecture) for tool path detection","status":"open","title":"T-005: Managed mode implementation"},"projectId":"035d2b4054bf","projectName":"tackle-box","ticketId":"T-00005","ts":1769706880,"tsIso":"2026-01-29T17:14:40.000Z","type":"ticket.created"}
{"actor":"hack@mac.lan","eventId":"743288be-e418-4525-9ae3-3b3ccdc27f83","payload":{"body":"## Summary\nImplement commands to enable/disable skills and MCP servers per-tool, and sync changes to managed tools.\n\n## Context\nOnce tools are in managed mode, users need fine-grained control over what's enabled where.\n\n## Requirements\n\n### enable command\n```bash\ntacklebox enable github --for cursor,claude\ntacklebox enable mcp:filesystem --for cursor\ntacklebox enable weather  # Enable for all managed tools\n```\n\nFlow:\n1. Validate item exists in index\n2. Update `enabledFor` array in index.json\n3. For each managed tool in `--for`:\n   - Skills: create symlink if not exists\n   - MCP: regenerate tool's mcp.json\n4. Save updated index\n\n### disable command\n```bash\ntacklebox disable github --for cursor\ntacklebox disable mcp:filesystem --for claude\ntacklebox disable weather  # Disable for all\n```\n\nFlow:\n1. Update `enabledFor` array (remove tools)\n2. For each managed tool:\n   - Skills: remove symlink\n   - MCP: regenerate tool's mcp.json without this server\n3. Save updated index\n\n### sync command\n```bash\ntacklebox sync              # Sync all managed tools\ntacklebox sync cursor       # Sync specific tool\ntacklebox sync --dry-run    # Show what would change\n```\n\nSync ensures managed tools match current index state:\n1. For each managed tool:\n   - Add missing symlinks for enabled skills\n   - Remove symlinks for disabled skills\n   - Regenerate mcp.json from servers.json + enabledFor\n\n### --dry-run output\n```\nWould sync cursor:\n  + Add symlink: github\n  - Remove symlink: old-skill\n  ~ Regenerate mcp.json (2 servers added, 1 removed)\n\nWould sync claude:\n  (no changes)\n```\n\n### Batch operations\n```bash\ntacklebox enable github weather codex --for cursor\n```\n\nEnable multiple items at once.\n\n## Acceptance Criteria\n- [ ] `enable` creates symlinks and updates index\n- [ ] `disable` removes symlinks and updates index\n- [ ] `sync` brings managed tools into alignment\n- [ ] `--dry-run` shows changes without applying\n- [ ] MCP config regenerated correctly on enable/disable\n- [ ] Batch enable/disable works\n\n## Effort\n2 days\n\n## Phase\nPhase 4: Managed Mode & Sync\n\n## Dependencies\n- T-005 (managed mode)\n- T-008 (adapters for MCP generation)","status":"open","title":"T-006: Enable/disable and sync commands"},"projectId":"035d2b4054bf","projectName":"tackle-box","ticketId":"T-00006","ts":1769706903,"tsIso":"2026-01-29T17:15:03.000Z","type":"ticket.created"}
{"actor":"hack@mac.lan","eventId":"d4cfb8f7-c207-4181-95e4-280e792dee71","payload":{"body":"## Summary\nDesign and implement the adapter architecture for bidirectional translation between tool-specific configs and canonical format.\n\n## Context\nFrom Council: \"Each tool has its own config format. tacklebox maintains bidirectional adapters. Version detection is worth it but keep it simple—only based on explicit version fields or reliable structural markers; otherwise fall back to latest with warnings.\"\n\n## Requirements\n\n### Directory structure\n```\nsrc/adapters/\n├── index.ts              # Registry + dispatch\n├── types.ts              # Shared types + interfaces\n├── cursor/\n│   ├── index.ts          # Cursor adapter\n│   └── ...\n├── claude/\n│   ├── index.ts\n│   ├── cli.ts            # Claude CLI specific\n│   └── desktop.ts        # Claude Desktop specific\n└── ...\n```\n\n### ToolAdapter interface\n```typescript\n// src/adapters/types.ts\n\ninterface ToolAdapter {\n  id: string;                    // 'cursor', 'claude-cli', etc.\n  name: string;                  // 'Cursor', 'Claude CLI'\n  versions: string[];            // ['1.0', '2.0']\n  \n  // Path detection\n  getDefaultPaths(): ToolPaths;\n  \n  // Version detection (minimal, explicit fields only)\n  detectVersion(configPath: string): Promise<string | null>;\n  \n  // Parse: tool format → canonical\n  parseMcp(config: unknown, version?: string): CanonicalMcpConfig;\n  parseSkills(skillsDir: string): Promise<CanonicalSkill[]>;\n  \n  // Generate: canonical → tool format\n  generateMcp(canonical: CanonicalMcpConfig, version?: string): unknown;\n  getSkillsSymlinkTarget(): string;  // Where to create symlinks\n}\n\ninterface ToolPaths {\n  mcp?: string;           // Path to MCP config file\n  skills?: string;        // Path to skills directory\n  config?: string;        // Path to main config (if separate)\n}\n\ninterface CanonicalMcpConfig {\n  servers: {\n    [name: string]: {\n      transport: 'stdio' | 'http' | 'sse';\n      command?: string;\n      args?: string[];\n      url?: string;\n      env?: Record<string, string>;\n      vendorExtensions?: Record<string, unknown>;\n    };\n  };\n}\n```\n\n### Adapter registry\n```typescript\n// src/adapters/index.ts\n\nconst adapters: Map<string, ToolAdapter> = new Map();\n\nexport function registerAdapter(adapter: ToolAdapter): void;\nexport function getAdapter(id: string): ToolAdapter | undefined;\nexport function getAllAdapters(): ToolAdapter[];\nexport function detectTool(path: string): ToolAdapter | undefined;\n```\n\n### Version detection rules\nFrom Council: \"Only detect when a version key exists; avoid heuristic drift.\"\n\n1. Check for explicit version field in config\n2. Check for known structural markers (field presence)\n3. If undetectable, assume latest and log warning\n4. Never use file modification dates for version detection\n\n### Fallback behavior\n```typescript\nif (!detectedVersion) {\n  console.warn(`Could not detect ${tool.name} version, assuming latest (${tool.versions[0]})`);\n  return tool.versions[0];\n}\n```\n\n## Acceptance Criteria\n- [ ] ToolAdapter interface defined and documented\n- [ ] Adapter registry works for register/get/detect\n- [ ] Version detection attempts explicit fields first\n- [ ] Clear warning when falling back to latest\n- [ ] At least one adapter implemented as reference\n\n## Effort\n2 days\n\n## Phase\nPhase 4.5: Translation Layers","status":"open","title":"T-007: Adapter architecture"},"projectId":"035d2b4054bf","projectName":"tackle-box","ticketId":"T-00007","ts":1769706933,"tsIso":"2026-01-29T17:15:33.000Z","type":"ticket.created"}
{"actor":"hack@mac.lan","eventId":"3544e520-98db-4f9b-bf46-90b0930058f0","payload":{"body":"## Summary\nImplement adapters for primary supported tools: Cursor, Claude (CLI + Desktop), Codex, and Clawdbot.\n\n## Context\nThese are the most common tools in the target user base. Each has different config formats and paths.\n\n## Requirements\n\n### Cursor adapter\n```typescript\n// src/adapters/cursor/index.ts\n\nid: 'cursor'\npaths:\n  mcp: ~/.cursor/mcp.json\n  skills: ~/.cursor/skills/\n  \nMCP format:\n{\n  \"mcpServers\": {\n    \"name\": { \"command\": \"...\", \"args\": [...] }\n  }\n}\n\nSkills: directory of SKILL.md files\n```\n\n### Claude CLI adapter\n```typescript\n// src/adapters/claude/cli.ts\n\nid: 'claude-cli'\npaths:\n  config: ~/.claude.json\n  skills: ~/.claude/skills/\n  \nMCP format (in config):\n{\n  \"mcpServers\": { ... }\n}\n```\n\n### Claude Desktop adapter\n```typescript\n// src/adapters/claude/desktop.ts\n\nid: 'claude-desktop'\npaths:\n  mcp: ~/Library/Application Support/Claude/claude_desktop_config.json\n  \nMCP format:\n{\n  \"mcpServers\": {\n    \"name\": {\n      \"command\": \"...\",\n      \"args\": [...],\n      \"env\": { ... }\n    }\n  }\n}\n\nNo skills directory (MCP only)\n```\n\n### Codex adapter\n```typescript\n// src/adapters/codex/index.ts\n\nid: 'codex'\npaths:\n  mcp: ~/.codex/mcp.json (or ~/.config/openai/codex.json)\n  skills: ~/.codex/skills/\n```\n\n### Clawdbot adapter\n```typescript\n// src/adapters/clawdbot/index.ts\n\nid: 'clawdbot'\npaths:\n  mcp: ~/.clawdbot/mcp.json\n  skills: ~/.clawdbot/skills/ AND ~/clawd/skills/\n  \nNote: Clawdbot has multi-agent structure, may have agent-specific skills\n```\n\n### Implementation per adapter\nEach adapter must implement:\n1. `getDefaultPaths()` — return known paths\n2. `detectVersion()` — check for version field, return null if not found\n3. `parseMcp()` — parse tool's MCP format to canonical\n4. `generateMcp()` — generate tool's MCP format from canonical\n5. Handle missing files gracefully\n\n### vendorExtensions preservation\nWhen parsing, capture unknown fields:\n```typescript\nconst { mcpServers, ...rest } = config;\n// rest goes to vendorExtensions\n```\n\nWhen generating, merge them back:\n```typescript\nreturn {\n  mcpServers: generatedServers,\n  ...original.vendorExtensions\n};\n```\n\n## Acceptance Criteria\n- [ ] Cursor adapter parses and generates correctly\n- [ ] Claude CLI adapter works\n- [ ] Claude Desktop adapter works\n- [ ] Codex adapter works\n- [ ] Clawdbot adapter works\n- [ ] All adapters preserve vendorExtensions\n- [ ] Missing file/directory handled gracefully\n- [ ] Round-trip test passes for each adapter\n\n## Effort\n3–4 days\n\n## Phase\nPhase 4.5: Translation Layers\n\n## Dependencies\n- T-007 (adapter architecture)","status":"open","title":"T-008: Tool adapters (Cursor, Claude, Codex, Clawdbot)"},"projectId":"035d2b4054bf","projectName":"tackle-box","ticketId":"T-00008","ts":1769706957,"tsIso":"2026-01-29T17:15:57.000Z","type":"ticket.created"}
{"actor":"hack@mac.lan","eventId":"e12d1dd6-4a27-429a-9638-ac665c60f3b6","payload":{"body":"## Summary\nImplement strict validation for snippet markers to prevent injection attacks and ensure consistent behavior.\n\n## Context\nFrom Council Security Officer: \"Ensure snippet replacement does not allow injection by validating marker names.\"\n\nSnippet markers use HTML comments: `<!-- tb:name -->...<!-- /tb:name -->`\n\n## Requirements\n\n### Valid marker names\nAllowed characters:\n- Alphanumeric: a-z, A-Z, 0-9\n- Hyphens and underscores: -, _\n- Forward slashes for scoping: /\n\nValid examples:\n- `tb:codingstyle`\n- `tb:global/boundaries`\n- `tb:myproject/context`\n- `tb:my-snippet_v2`\n\n### Invalid patterns to reject\n- Path traversal: `tb:../../../etc/passwd`\n- Empty names: `tb:`\n- Special characters: `tb:foo;bar`, `tb:foo$(cmd)`\n- Whitespace: `tb:foo bar`\n- Leading/trailing slashes: `tb:/foo`, `tb:foo/`\n\n### Validation function\n```typescript\nfunction validateMarkerName(name: string): { valid: boolean; error?: string } {\n  if (!name) return { valid: false, error: 'Empty marker name' };\n  if (name.includes('..')) return { valid: false, error: 'Path traversal not allowed' };\n  if (!/^[a-zA-Z0-9_-]+(\\/[a-zA-Z0-9_-]+)*$/.test(name)) {\n    return { valid: false, error: 'Invalid characters in marker name' };\n  }\n  return { valid: true };\n}\n```\n\n### Error messaging\nWhen invalid marker found:\n```\nError: Invalid snippet marker \"tb:../foo\" in CLAUDE.md:15\n  Path traversal (..) not allowed in marker names.\n  Valid format: tb:name or tb:scope/name\n  Examples: tb:codingstyle, tb:global/boundaries\n```\n\n### Integration points\n1. Snippet sync command — validate before replacing\n2. Snippet show/list — validate when reading\n3. Create command — validate name input\n\n## Acceptance Criteria\n- [ ] Path traversal rejected\n- [ ] Special characters rejected\n- [ ] Clear error messages with line numbers\n- [ ] Valid scoped names work (tb:global/foo)\n- [ ] Regex covers all edge cases\n\n## Effort\n1 day\n\n## Phase\nPhase 5: Snippets & Templates\n\n## Security Note\nThis is a security boundary — marker names must not allow arbitrary file access or command injection.","status":"open","title":"T-009: Snippet marker validation"},"projectId":"035d2b4054bf","projectName":"tackle-box","ticketId":"T-00009","ts":1769706983,"tsIso":"2026-01-29T17:16:23.000Z","type":"ticket.created"}
